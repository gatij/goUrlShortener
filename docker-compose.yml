version: '3'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
      - CODE_LENGTH=6
      - GIN_MODE=release
    restart: unless-stopped
